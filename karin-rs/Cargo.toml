[package]
name = "karin-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.29", default-features = false, features = ["glow", "default_fonts"] }
egui = "0.29"
notify = "6.1"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
